# library-management-system
A library management backend with a normalized relational schema — books, authors, categories, physical copies, and members — plus a borrow/return workflow with real business rules (availability checks, due dates, overdue tracking). Built with Flask, SQLAlchemy, and PostgreSQL.
